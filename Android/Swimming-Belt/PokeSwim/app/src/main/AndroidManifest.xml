<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.lukile.pokeswim">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="ANDROID.PERMISSION.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".tuto.TutoActivity"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".signin.SigninActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".signup.SignupActivity"
            android:label="@string/title_activity_signup"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MenuActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".performance.ListPerformanceActivity"
            android:label="@string/action_performances"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SplashscreenActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".programs.ProgramActivity"
            android:label="@string/action_programs"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".programs.ProgramSpringActivity"
            android:label="@string/action_programs"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".programs.ProgramSummerActivity"
            android:label="@string/action_programs"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".programs.ProgramWinterActivity"
            android:label="@string/action_programs"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".profile.ProfileActivity"
            android:label="@string/action_profile"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>