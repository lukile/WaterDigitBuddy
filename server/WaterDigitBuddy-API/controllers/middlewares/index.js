const authentication = require("./authentication");
const authByDevice = require("./authByDevice");
const graphql = require("./graphql");


module.exports = {
    authByDevice,
    authentication,
    graphql
};
